# Arduino_LED_ws
A ROS 2 node for controlling LEDs on an Arduino via serial communication. The node subscribes to the led_Control topic and sends commands over a serial connection, allowing real-time LED control from ROS 2.
